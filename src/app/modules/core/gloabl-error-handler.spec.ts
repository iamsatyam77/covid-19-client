import { GloablErrorHandler } from './gloabl-error-handler';

describe('GloablErrorHandler', () => {
  it('should create an instance', () => {
    expect(new GloablErrorHandler()).toBeTruthy();
  });
});
